// c++

#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {

    // Your code here
    int x = 5;
    int a, b;
    cin >> a >> b;
    int ans = (2 * (x + a) + b) / 2 - x;
    cout << ans;
    return 0;
}
